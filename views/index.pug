extends layout.pug

block content

  article.portfolio-intro

    
    h1 My Portfolio

    
    p Here I showcase all the projects I have completed through my Full Stack Tech Degree.  Through completing these projects  I have gained knowledge of Node.js, Express and Pug and React. Stay tuned for more projects.


  article.portfolio-project-thumbs
    .projects-box.box.grid
      each project in projects 
        
        .cell
          
          a(href=`/projects/${project.id}`)
            
            img.thumbnail(src=`${project.image_urls[0]}`, alt=`${project.project_name}`)
            
            h5= project.project_name
   
    